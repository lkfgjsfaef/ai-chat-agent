# 企业级 AI 智能助手系统详解与实战指南

> 这份文档不是简单的功能清单，而是站在“初学者能看懂、面试时能讲清、写到简历里有说服力”的角度，系统讲解这个项目到底做了什么、为什么要这么做、每个高含金量功能解决了什么问题，以及它们在代码里是怎么落地的。

---

## 1. 项目总览

### 1.1 这个项目到底是什么

这是一个基于 Spring Boot 构建的企业级 AI 智能助手系统。它不是单纯“调一下大模型 API 然后把结果返回给前端”的玩具项目，而是把一个真正可用的 AI 系统所需要的几条核心链路都补齐了：

- 对话链路：支持流式聊天、上下文管理、会话管理。
- Agent 链路：支持工具调用，让模型不只是“会说话”，还会“做事”。
- RAG 链路：支持知识上传、解析、向量检索、关键词检索、重排、缓存、知识域隔离。
- 高可用链路：支持模型降级、排队削峰、分布式并发控制、标准熔断。
- 安全与治理链路：支持 JWT 鉴权、角色权限、分级限流、知识库权限边界。
- 观测与运维链路：支持运行时指标、历史快照、健康评估、管理接口。

如果用一句话概括这个项目，可以这样说：

> 这是一个把“大模型调用、Agent、RAG、并发治理、权限控制、运维可观测”整合到一起的企业级 AI 后端系统。

### 1.2 为什么这个项目有含金量

很多 AI 项目之所以显得“含金量一般”，不是因为它们用了小模型，而是因为它们通常只做了下面这一步：

1. 前端输入一句话。
2. 后端把这句话转发给模型。
3. 模型回一句话。
4. 完了。

这类项目的问题是：

- 没有知识库，模型容易胡说。
- 没有并发治理，请求一多就崩。
- 没有降级和熔断，某个模型出问题整个系统就挂。
- 没有权限边界，普通用户和管理员没有区别。
- 没有观测能力，系统异常时没人知道问题出在哪。

而本项目补的，恰恰都是企业落地时真正会遇到的问题。所以它的含金量不在“功能多”，而在“功能背后解决的是生产环境问题”。

---

## 2. 建议先理解的基础概念

如果你是初学者，建议先把这一节看明白，再看后面的每个功能。

### 2.1 什么是 LLM

LLM 就是大语言模型，比如 GPT、GLM、DeepSeek 等。

它的特点是：

- 很擅长理解语言、生成语言。
- 能做总结、翻译、写代码、问答。
- 但它并不是万能的。

它的天然缺点也很明显：

- 不知道最新信息。
- 不知道你公司内部知识。
- 容易出现幻觉。
- 并发上来时成本高、稳定性差。

所以企业做 AI 系统，不能只接一个模型，还要围绕它补很多“工程能力”。

### 2.2 什么是 Agent

Agent 可以理解成“会调用外部工具的大模型”。

普通模型只能靠自己脑子回答问题。
Agent 模型可以这样工作：

1. 先判断用户的问题自己能不能直接回答。
2. 如果不能，就决定要不要调用工具。
3. 工具执行后，把工具结果再交给模型组织成最终回复。

比如用户问“现在几点了”，模型自己不知道真实时间，但它可以调用“时间工具”。

### 2.3 什么是 RAG

RAG 全称是 Retrieval-Augmented Generation，也就是“检索增强生成”。

你可以把它理解成：

- 先去知识库里查资料。
- 再把资料连同问题一起交给模型回答。

这样模型就不是凭空猜，而是“带着证据作答”。

RAG 的核心目标就是减少幻觉，让回答更贴近企业真实知识。

### 2.4 什么是 SSE

SSE 是 Server-Sent Events，服务器推送事件。

它适合做 AI 打字机效果，因为：

- 后端可以一边拿到模型返回的文本碎片，一边推给前端。
- 用户不需要一直等到整段话生成完。
- 前端体验会更像 ChatGPT。

### 2.5 什么是熔断、降级、限流、削峰

这些词在面试里很常见，也很容易混。

- 限流：限制单位时间内请求数，防止系统被打爆。
- 削峰：请求太多时先排队，不要一下全部压到后端。
- 降级：主模型失败时换备用模型。
- 熔断：某个模型连续失败时，暂时不要再调用它，等它恢复后再试。

你可以把它们理解成：

- 限流是门口保安。
- 削峰是大厅排队机。
- 降级是备用专家。
- 熔断是“这个专家今天状态不行，先别找他了”。

---

## 3. 项目整体架构图景

从大的数据流看，这个系统可以分成 6 层：

### 3.1 接入层

负责接收前端请求，比如：

- 聊天请求
- 重新生成请求
- 文件上传请求
- 管理后台查询请求

典型类：

- `ChatController`
- `FileUploadController`
- `AdminController`

### 3.2 鉴权与治理层

负责做：

- JWT 身份校验
- 角色识别
- 接口限流

典型类：

- `JwtInterceptor`
- `RateLimitFilter`

### 3.3 对话编排层

负责组装发送给模型的最终上下文，包括：

- 系统提示词
- 历史消息
- 摘要
- RAG 检索结果
- 工具调用结果

典型类：

- `MemoryServiceImpl`
- `TokenServiceImpl`

### 3.4 模型调度层

负责：

- 选择模型
- 执行降级链
- 控制并发
- 做排队
- 统计运行态指标
- 做标准熔断

典型类：

- `AiModelRouterServiceImpl`

### 3.5 知识与工具层

负责：

- 文件解析
- 向量化入库
- 关键词检索
- 工具执行

典型类：

- `ChatAttachmentServiceImpl`
- `ToolRegistry`
- `AbstractOpenAiCompatibleModel`

### 3.6 运维与观测层

负责：

- 运行时指标
- 历史快照
- 健康评估
- 告警摘要

典型类：

- `RuntimeMetricsHistoryServiceImpl`
- `RuntimeHealthServiceImpl`

---

## 4. 高含金量功能一：Agent 工具调用

这一部分是“让模型从只会说话，变成能真正做事”。

### 4.1 为什么要做这个功能

大模型训练完成后，知识就基本固定了。

所以当用户问下面这类问题时，模型天然会有问题：

- 现在几点了？
- 今天天气怎么样？
- 某个系统当前状态如何？

因为这些信息是动态变化的，模型不能仅靠训练时学到的内容来回答。

这时就需要工具调用。

### 4.2 用最简单的话解释它的工作原理

把整个过程想象成：

- 用户是老板。
- 后端是秘书。
- 大模型是专家。
- 工具是秘书能调用的外部资源。

流程是这样的：

1. 老板提问。
2. 秘书把问题和工具说明书一起给专家。
3. 专家发现自己不能直接回答，于是说“你去帮我调用某个工具”。
4. 秘书执行这个工具。
5. 秘书把工具结果再反馈给专家。
6. 专家基于工具结果生成最终自然语言答案。

### 4.3 项目里是怎么落地的

项目定义了统一工具接口 `ToolHandler`，并通过 `ToolRegistry` 自动注册工具。

这样做的好处是：

- 工具能力可扩展。
- 不需要每次手动维护一个工具列表。
- Spring 容器启动时会自动把所有工具收集起来。

在底层模型适配类 `AbstractOpenAiCompatibleModel` 里，模型返回的 SSE 数据会被持续解析。

如果发现返回的是普通文本：

- 直接推给前端。

如果发现返回的是 `tool_calls`：

- 不直接展示给前端。
- 先在后端把它们拼接成完整参数。
- 再本地执行工具。
- 再递归调用模型生成最终回答。

### 4.4 这里面真正难的点是什么

难点不是“调一下工具”，而是下面这些细节：

#### 4.4.1 工具参数是流式碎片

模型不是一次性把完整参数给你，而是可能一段一段地吐：

- 第一段给你一半 JSON
- 第二段再给你剩下一半

所以后端不能马上执行，必须先拼完整。

#### 4.4.2 工具调用可能不止一个

模型一次可能返回多个工具调用。

本项目做了并行执行优化：

- 多个 Tool Call 会并发执行。
- 但回填消息时仍然保持模型原始顺序。

这解决了两个问题：

- 提高速度。
- 保证上下文顺序稳定，不让模型“看乱了”。

#### 4.4.3 必须防止死循环

如果模型一直要工具，理论上可能无限递归。

项目里加了递归深度上限，超过阈值就中断，避免：

- 内存被耗尽
- 请求长时间不返回
- 异常工具链把系统拖死

### 4.5 为什么这个功能有含金量

因为它体现了你不仅会调模型，还懂：

- 流式协议解析
- 工具调用协议
- 并发执行
- 递归控制
- 优雅降级

这比“普通聊天机器人”高级很多。

---

## 5. 高含金量功能二：流式对话与 SSE 事件编排

### 5.1 为什么要用流式返回

如果等模型整段话生成完再返回，用户体验会很差：

- 等待时间长
- 没反馈
- 用户会误以为系统卡住了

所以项目采用 SSE 做流式输出。

### 5.2 SSE 在这个项目里不只是“打字机效果”

很多项目的 SSE 只是单纯输出文本。
本项目的 SSE 还承载了状态事件，比如：

- 排队中
- 已获取执行资格
- 释放执行资格
- 错误事件

这意味着 SSE 不再只是“传文本”，而是一个轻量级事件总线。

### 5.3 这背后的价值

当模型请求进入排队时，前端可以立刻知道：

- 当前是不是还没开始生成
- 前面还有多少请求
- 已经等了多久

这样就不会出现“前端什么都不显示，用户以为系统坏了”的问题。

### 5.4 这个功能为什么也值得写进简历

因为它说明你理解了：

- 命名事件和普通文本事件的区别
- 前后端如何配合解析 SSE 协议
- 如何把后端运行状态安全地映射到前端体验

这不是简单的页面效果，而是完整的交互协议设计。

---

## 6. 高含金量功能三：RAG 知识库链路

这一部分是整个项目的核心之一。

### 6.1 为什么必须做 RAG

大模型最大的问题之一是“胡说八道”。

比如公司内部制度、产品手册、流程文档，这些内容大模型训练时根本没见过。
如果不做 RAG，模型只能猜。

一旦猜错：

- 用户会失去信任。
- 企业场景根本不敢上线。

所以 RAG 的作用是：

> 让模型回答问题时，先查证据，再回答。

### 6.2 从零讲清楚 RAG 的完整流程

RAG 不只是“把文件丢进去”这么简单，它至少包含 5 步。

#### 6.2.1 文档解析

用户上传 PDF、Word、文本文件后，系统先把里面的原始文字提取出来。

项目使用 Apache Tika 做解析。

它的作用是：

- 统一处理多种文件格式。
- 尽量把结构化或半结构化文件转成可处理文本。

#### 6.2.2 文本分块

为什么要分块？

因为文档可能很长，而模型和向量库都更适合处理较小粒度的文本片段。

如果不分块：

- 检索粒度太粗。
- 召回结果不精准。
- 上下文很容易超长。

所以系统会把文档切成多个 chunk。

这里一定要特别理解一个关键事实：

> RAG 的效果，很多时候不是输在模型不够强，而是输在切分方式不对。

因为模型最后真正看到的不是“整份文档”，而是“被切完后的很多小片段”。

如果片段切得不合理，就会出现下面这些经典问题：

- 一个完整操作流程被拆成了前后两半。
- 一个章节标题和正文被切开，模型看不出这段话属于什么主题。
- 一个 FAQ 的问题和答案被拆开，导致只召回答案片段的一半。
- 一个回滚方案被切到另外一个 chunk 里，模型只看到执行步骤，看不到兜底方案。

所以“分块”不是简单的数据预处理，而是 RAG 质量的第一道门槛。

#### 6.2.3 向量化入库

分块后的文本会被转成向量，存入 `RedisVectorStore`。

你可以把向量理解成“文字在数学空间里的坐标”。

语义相近的句子，在空间里距离就更近。

#### 6.2.4 检索召回

用户提问后，系统不会立刻把问题丢给模型，而是先做检索：

- 一路走向量检索，找语义相近内容。
- 一路走 BM25 关键词检索，找字面匹配内容。

最后再把两路结果合并。

#### 6.2.5 把证据交给模型

经过筛选后的知识片段会被拼成上下文，追加到模型输入中。

这时模型就不是“裸答”，而是“参考知识后作答”。

你可以把这个过程理解成一次“开卷考试”：

1. 用户先给出问题。
2. 系统先去知识库找资料。
3. 只保留更相关的资料片段。
4. 再让模型基于这些资料组织答案。

如果没有前面的检索和筛选，模型就是闭卷硬猜。
如果检索到了资料，但资料本身切得不好、排序不准、提示词约束太弱，最终回答一样会出问题。

### 6.3 项目里为什么用了双路召回

只用向量检索有一个问题：

- 它擅长理解语义。
- 但对型号、缩写、专有名词不一定稳。

只用关键词检索也有问题：

- 它找字面命中很准。
- 但无法理解近义表达和语义相似。

所以项目采用混合检索：

- 向量检索负责“懂意思”
- BM25 负责“认关键词”

这就是典型的企业级 RAG 方案，而不是最基础的单路召回。

### 6.4 项目里为什么还要做并行检索

如果先做向量检索，再做关键词检索，整体响应时间会被拉长。

所以项目里把两路召回改成了 `CompletableFuture` 并行执行，并加了：

- 超时兜底
- 异常降级

即使某一路暂时失败，另一条路仍能工作，保证整体 RAG 能继续跑。

这体现的是：

- 性能优化
- 高可用思维

### 6.5 为什么要做 Rerank 重排

召回阶段拿回来的结果，通常只是“可能相关”。

但真正喂给模型的片段数量是有限的，所以必须再做一次“优中选优”。

项目里用重排模型做了这件事：

1. 输入用户问题。
2. 输入多个候选片段。
3. 给每个片段打分。
4. 只保留更相关的内容。

这样能明显提升答案质量，减少无关上下文污染模型。

### 6.6 为什么要做低分过滤和无命中降级

如果检索出来的结果相关性很低，但你还是强行塞给模型，会出现两个坏处：

- 模型被错误信息干扰。
- 用户以为回答“有知识依据”，其实依据是错的。

所以项目里会：

- 对重排结果做最低分阈值过滤。
- 如果最终没有可靠知识，则明确走“无命中降级”。

这个设计很重要，因为它体现了“宁可不引用，也不乱引用”的工程原则。

### 6.7 为什么要做 Query Rewrite

用户很多问题都不是完整表达，比如：

- “那这个呢？”
- “上一个版本的限制是什么？”
- “如果是管理员呢？”

这种问题只看最后一句，很难检索。

所以项目在检索前会做 Query Rewrite，也就是把：

- 最近几轮用户问题
- 会话摘要
- 当前追问

整合成一个更适合检索的查询文本。

这会显著提升追问场景下的 RAG 命中率。

你可以这样理解：

- 用户真正说出口的问题，往往是“省略版”。
- 检索系统更喜欢“信息完整版”。

比如用户说：

- “那这个怎么配？”

人类结合上下文，可能知道他问的是 Redis 限流配置。
但检索系统如果只看这句话，几乎没有有效关键词。

所以 Query Rewrite 的目标不是帮用户“换个问题”，而是把用户真实想问的内容补全出来。

### 6.7.1 本项目最新补上的查询扩展

在这个项目的最新版本里，RAG 不再只构造一个检索 query，而是进一步做了“多查询扩展”。

这一步主要是为了解决一个很常见但很隐蔽的问题：

- 用户提问是口语化表达。
- 文档通常是书面化表达。
- 两者说的是一回事，但字面差别很大。

所以系统现在会这样做：

1. 先生成一个基础检索 query。
2. 再用 LLM 生成 3 到 5 个不同表述的 query 变体。
3. 每个变体都分别参与向量检索和 BM25 检索。
4. 把多路召回结果合并去重后，再统一做 rerank。

这一步的价值在于，它能同时覆盖：

- 口语说法
- 书面说法
- 配置说法
- 排障说法
- 操作流程说法

比如用户问：

- “数据库挂了咋办？”

系统更有机会扩展出：

- “MySQL 服务异常排查”
- “数据库故障处理流程”
- “MySQL 宕机恢复方案”

这样检索命中率就会明显提升。

### 6.8 为什么要做 RAG 意图门控

并不是所有问题都值得走 RAG。

比如：

- “你好”
- “帮我写个 Java 排序”
- “今天天气怎么样”

这些问题如果每次都去查知识库，会带来：

- 额外延迟
- 不必要的成本
- 无效召回

所以项目加入了轻量级意图门控：

- 判断是否像知识问答
- 判断是否有附件上下文
- 判断是否是追问
- 识别闲聊/工具型问题

只有更像知识问题时才触发检索。

这一步很有含金量，因为它不只是“让系统能检索”，而是“让系统知道什么时候该检索”。

### 6.9 为什么要做 RAG 缓存

在真实系统里，用户经常会：

- 连续问同一主题
- 重复追问
- 多次测试同一个答案

如果每次都重新做检索、重排，会浪费资源。

所以项目做了会话级 RAG 短期缓存：

- 命中过去的检索结果时直接复用。
- 缓存的不只是“有命中”，还包括“无命中”。

很多人只缓存成功结果，不缓存无命中。
但无命中缓存同样重要，因为它能避免系统对同一无效问题反复做昂贵检索。

### 6.10 为什么要做增量摘要

随着聊天轮数变多，上下文会越来越长。

如果每次都把很久之前的聊天记录原样发送给模型，会出现：

- 成本高
- 上下文超长
- 模型注意力分散

所以项目加入了摘要机制。

但它不是每次都重做整段摘要，而是做“增量摘要”：

- 数据库记录已经摘要到哪条消息。
- 只对新增消息做补充总结。
- 摘要边界持续推进。

这比“每次重新总结最近 20 条消息”高级得多，因为它真正解决了长会话成本问题。

### 6.11 本项目针对 RAG 三大坑做了哪些治理

这一节非常重要，因为它就是从“基础 RAG”走向“更像企业级 RAG”的关键分水岭。

#### 6.11.1 第一坑：文档机械切分

很多项目直接按固定长度切 chunk，比如：

- 每 500 字切一块
- 每 800 token 切一块

这样虽然实现简单，但会严重破坏文档语义结构。

比如一份“数据库切换方案”文档，原本结构可能是：

1. 前置检查
2. 切换步骤
3. 回滚方案

如果只按长度切，可能就会变成：

- 第一块：前置检查的一半
- 第二块：前置检查后半段 + 切换步骤开头
- 第三块：切换步骤中段
- 第四块：回滚方案

这样用户问“切换前要检查什么”时，模型可能只召回第二块，而第二块刚好又是不完整内容。

所以项目现在已经升级为：

- 优先按 Markdown 标题、数字编号标题、中文章节标题切分。
- 给每个 chunk 加 `source_path` 和 `section_title`。
- 只有当单个章节本身太长时，才在章节内部再做 token 级切分。

这意味着系统现在更尊重“文档原始结构”，而不是粗暴地按长度拆碎。

#### 6.11.2 第二坑：语义鸿沟

所谓语义鸿沟，就是：

- 用户问题是口语。
- 文档内容是书面语。
- 两边其实说的是一件事，但表面词不一样。

所以项目现在的治理不是只加一个 rerank 就结束，而是做了四层增强：

1. Query Rewrite：把问题从“省略版”补成“可检索版”。
2. Query Expansion：生成多种 query 变体。
3. Hybrid Search：向量检索 + BM25 双路召回。
4. Rerank：把候选结果再精排。

你可以把它理解成：

- 第一层解决“问题太短”。
- 第二层解决“表达不同”。
- 第三层解决“通道太单一”。
- 第四层解决“召回回来了但排序不准”。

这就形成了一套比较完整的检索质量治理链路。

#### 6.11.3 第三坑：Prompt 太弱

很多 RAG 项目的 Prompt 只有一句：

- “请根据以下资料回答问题”

这看起来像做了约束，但实际上非常弱。模型仍然可能：

- 脑补片段里没写的内容。
- 明明证据不足，还硬给一个完整答案。
- 不标来源，让人看不出哪些话是真的有依据，哪些是模型自己发挥。

所以项目现在把 Prompt 收紧成 grounded 模式，也就是“有证据才说，没证据就明确说没有”。

当前规则包括：

1. 只能基于检索到的知识片段回答。
2. 证据不足时明确回答“无相关信息”或“知识库未提供足够证据”。
3. 关键结论、原因、步骤都必须附来源。
4. 按问题类型动态调整输出格式。

这比单纯“给模型一点参考资料”要高级很多，因为它开始真正约束模型行为。

### 6.12 一次完整的 RAG 问答到底怎么跑

为了帮助你真正建立系统级理解，下面把一次典型问答完整串起来。

假设用户问：

- “数据库挂了咋办？”

系统内部会经历这些步骤：

#### 第一步：判断要不要走 RAG

系统先判断这是不是知识型问题。

如果用户只是说：

- “你好”
- “谢谢”

那就没有必要查知识库。

而“数据库挂了咋办”明显更像知识型排障问题，所以会进入 RAG 流程。

#### 第二步：构造基础查询

系统会把：

- 当前问题
- 最近几轮用户问题
- 历史摘要

拼成一个更完整的基础检索 query。

#### 第三步：生成多个 query 变体

系统再用较轻量模型，把问题扩展成多个不同表述，比如：

- 数据库挂了怎么处理
- MySQL 服务异常排查步骤
- 数据库故障恢复流程

#### 第四步：多路检索

每个 query 变体都会参与：

- 向量检索
- BM25 检索

这样做的目标是尽量扩大召回覆盖面。

#### 第五步：按知识域和权限过滤

召回回来的片段不是全都能用。

系统会根据当前会话上下文判断：

- 哪些是 `session` 知识
- 哪些是 `user` 知识
- 哪些是 `global` 知识

并只保留当前用户真正可访问的片段。

#### 第六步：合并去重与重排

不同 query、不同检索通道可能会召回同一内容。

系统会先合并去重，再统一走 rerank，保证留下来的片段更相关。

#### 第七步：阈值过滤

如果某些片段虽然召回了，但相关性太低，也不会继续保留。

这一步体现的是一个非常重要的原则：

- 宁可少给，也不要乱给。

#### 第八步：构造严格证据 Prompt

系统会把最终保留下来的知识片段拼进一个系统提示里，并明确要求模型：

- 只能依据这些片段回答
- 没证据就明确说没有
- 每个关键结论都带来源

#### 第九步：按问题类型组织答案

如果是排障类问题，系统更适合要求模型按：

- 故障判断
- 可能原因
- 排查步骤
- 处理建议

的结构输出。

如果是概念类问题，则更适合：

- 先下定义
- 再分点展开

#### 第十步：流式返回给前端

最后模型生成的答案会通过 SSE 持续推给前端，用户看到的是一个流式、带证据引用的回答过程。

### 6.13 为什么结构化来源路径很重要

现在项目里每个 chunk 不只是有文本内容，还会附带更结构化的来源路径，比如：

- `MySQL运维手册 > 主从切换 > 前置检查`
- `系统配置说明 > Redis限流 > 配置项解释`

这个能力有三个很大的价值：

#### 6.13.1 提升模型理解能力

模型不再只是看到一段孤立文字，而是知道：

- 这段文字属于哪份文档
- 来自哪个章节
- 在整份文档结构中的位置是什么

这能显著减少断章取义。

#### 6.13.2 提升回答可解释性

用户看到来源时，不再只是看到“片段 1”这种模糊概念，而是看到更像真实目录结构的文档来源。

这会让回答更可信。

#### 6.13.3 提升后续治理能力

以后如果你还要做：

- 来源点击跳转
- 章节级治理
- 片段级删除
- 更细粒度的引用展示

这些能力都会依赖结构化来源路径。

---

## 7. 高含金量功能四：三层知识域设计

这是本项目最近非常关键的一次升级，也是很适合写进简历的亮点。

### 7.1 什么是三层知识域

项目把知识分成 3 层：

- `session`：会话级知识，只在当前会话里生效。
- `user`：用户级知识，属于某个用户的长期私有知识。
- `global`：全局知识，整个系统都能共享。

### 7.2 为什么这件事有含金量

很多 RAG 项目只有一种知识库：

- 谁上传都放一起
- 谁都能搜到

这样做的问题非常大：

- 容易串库
- 容易泄露用户私有知识
- 无法表达企业里的公共知识与个人知识差异

三层知识域本质上是在做“知识权限和知识作用范围设计”，这已经不只是检索问题，而是系统架构问题。

### 7.3 这三层分别适合什么场景

#### 7.3.1 session 知识

适合临时上传场景，比如：

- 当前会话上传一份 PDF，让 AI 只在这次会话里参考。

特点是：

- 生命周期短
- 作用范围小
- 不应该污染长期知识库

#### 7.3.2 user 知识

适合个人长期资料，比如：

- 用户自己的工作手册
- 个人项目资料
- 自己的长期知识资产

特点是：

- 长期存在
- 只属于当前用户
- 不应该被别人看到

#### 7.3.3 global 知识

适合全系统共享内容，比如：

- 公司制度
- 产品说明书
- 通用 FAQ

特点是：

- 全员共享
- 权限需要更严格
- 通常只允许管理员维护

### 7.4 项目里是怎么落地的

项目对知识链路做了完整闭环改造：

- 上传接口支持显式传 `scope`
- 附件实体落库 `scope` 与 `userId`
- 向量元数据同步写入 `session_id`、`user_id`、`scope`
- 检索阶段按知识域过滤
- 管理接口按权限返回可见知识

这说明三层知识域不是“只在检索时写个 if 判断”，而是贯穿：

- 上传
- 落库
- 向量入库
- 检索
- 列表查询
- 删除
- 缓存失效

如果站在企业系统角度再往深一点理解，这一步其实解决的是“知识污染”和“知识泄露”问题。

比如：

- A 用户上传了一份测试文档。
- 如果没有作用域隔离，B 用户也可能检索到它。

再比如：

- 某次会话里上传了一份只在当前问题里有意义的材料。
- 如果它进入了全局知识域，就会污染所有后续回答。

所以三层知识域不只是为了“方便分类”，而是为了保证：

- 会话知识不串到别的会话
- 用户知识不泄露给其他用户
- 全局知识有更严格的写入和维护权限

这已经是一个知识治理问题，而不只是检索问题。

### 7.5 为什么还要做知识域配额

如果三层知识都无限量召回，会有两个问题：

- 上下文被大量知识淹没
- 全局知识可能压过更贴近当前问题的会话知识

所以项目里给不同知识域设置了不同召回配额：

- 会话知识更多
- 用户知识次之
- 全局知识更少

这背后的思路是：

> 越接近当前上下文的知识，优先级越高。

这属于“知识域优先级设计”，非常像企业系统里的真实做法。

---

## 8. 高含金量功能五：知识库治理与删除闭环

### 8.1 为什么知识删除不是删数据库记录那么简单

RAG 系统里，一个附件通常会对应多份数据：

- 数据库记录
- 磁盘原文件
- 向量库里的多个 chunk
- 相关缓存

如果你只是删了数据库记录，会出现：

- 文件还在磁盘里
- 向量库里还能被搜到
- 缓存还在命中旧知识

这就是典型的“脏数据闭环没做完”。

### 8.2 项目里怎么解决这个问题

项目给每个附件都维护了 `vectorDocIds`，也就是：

- 这个附件切成了哪些 chunk
- 这些 chunk 在向量库里的唯一 ID 是什么

这样删除附件时，就可以做到精确删除：

1. 删向量库中的对应 chunk
2. 删物理文件
3. 删数据库记录
4. 按知识域清理相关 RAG 缓存

### 8.3 为什么这一步有含金量

因为它体现的是“知识资产治理”思路，而不是“上传能用就行”。

很多人做 RAG 项目只做：

- 上传
- 检索

但企业里真正麻烦的是：

- 知识更新
- 知识删除
- 索引同步清理
- 缓存同步失效

你把这一套闭环做完，项目的完成度就会高很多。

### 8.4 为什么还要做知识管理接口

如果只有上传接口，没有列表接口和删除接口，知识库其实不可运营。

所以项目还增加了：

- 知识分页查询
- 按 `scope` 过滤
- 按关键字搜索
- 按角色控制可见范围

这样知识库才从“技术演示能力”变成“可管理的系统能力”。

### 8.5 为什么还要记录 `vectorDocIds`

这个点非常适合拿去面试讲，因为它体现了你真的理解“向量库不是数据库表里的一列字段”。

一个附件进入知识库后，通常不会只对应一条向量记录，而是会拆成多个 chunk。

所以如果你不记录：

- 这个附件到底对应哪些 chunk

删除时就会很尴尬：

- 你知道删了附件记录
- 但你不知道该删向量库中的哪些片段

所以项目把每个附件对应的向量文档 ID 列表持久化下来，这样删除时才能精确回收向量数据。

这是一种很典型的“索引回指”设计，它的价值在于：

- 删除更干净
- 不会遗留幽灵知识
- 后续片段治理也更方便

---

## 9. 高含金量功能六：模型路由、自动降级与保底链路

这是另一个非常能体现后端工程能力的模块。

### 9.1 为什么不能只接一个模型

如果系统只依赖单一模型，会面临很多风险：

- 某个模型接口偶发超时
- 某个模型短时间不可用
- 某个模型当天响应变慢
- 某个模型价格过高，不适合所有场景

所以企业里通常不会让所有请求死磕一个模型。

### 9.2 项目里做了什么

项目实现了：

- 主模型可配置
- GLM 模型轮换池
- 自动降级链
- `deepseek` 作为最终保底

大致逻辑是：

1. 先尝试用户指定或系统默认主模型。
2. 如果主模型失败，则自动切到其他候选模型。
3. 如果都失败，再尝试保底模型。

### 9.3 为什么这件事重要

因为它意味着系统不是“某个模型可用时才能工作”，而是：

> 某个模型出问题，系统仍尽量保证服务可用。

这就是高可用思维。

### 9.4 这里的难点是什么

难点不在于写个模型名列表，而在于：

- 候选模型是否真的启用
- 数据库里有没有对应配置
- endpoint 是否有效
- 调用失败后如何无缝切换
- 切换时如何不让前端看到太多异常细节

项目里这些都做了判断，而不是单纯写死链路。

---

## 10. 高含金量功能七：分布式并发闸门与请求排队削峰

这是非常能体现“企业级”味道的一块。

### 10.1 为什么模型调用需要并发治理

大模型请求不是普通接口请求，它们通常具备这些特点：

- 响应时间长
- 占用资源重
- 外部依赖不稳定
- 单次成本高

如果大量请求同时打进来，会出现：

- 响应变慢
- 模型接口被打爆
- 线程资源耗尽
- 用户体验极差

所以必须做并发控制。

### 10.2 最基础的方案为什么不够

最简单的方法是本机加一个 `Semaphore`。

但它有一个明显问题：

- 只对当前进程有效

如果系统部署成多实例，就会出现每台机器各自放行，最终总并发仍然失控。

### 10.3 项目里做了什么升级

项目把单机并发控制升级成了 Redisson 分布式闸门：

- 使用 `RSemaphore` 控制执行资格
- 使用 `RAtomicLong` 统计活跃请求和等待请求
- 多实例之间共享同一套并发额度

这意味着：

- 整个集群共享一个总并发阈值
- 不是每台机器各管各的

### 10.4 为什么还要做排队

如果拿不到执行资格，系统有两种选择：

- 直接拒绝
- 允许短暂排队等待

项目选择了更友好的方式：

- 先排队
- 到了超时时间还没轮到，再失败
- 队列满了则直接拒绝

这样既能保护系统，又不会让用户体验过于粗暴。

### 10.5 为什么还要把排队状态通过 SSE 推给前端

因为用户最怕的是“不知道系统在干嘛”。

如果你只是让用户一直等，他会以为：

- 页面卡了
- 接口挂了
- 网络断了

所以项目在获取执行资格的过程中，会把这些状态发给前端：

- 已进入队列
- 前方还有多少请求
- 已等待多久
- 已获取执行资格

这就把后端的并发治理状态转成了用户可感知体验。

这里还有一个很容易被忽略但很重要的点：

很多系统虽然做了排队，但前端完全不知道当前到底是在：

- 排队中
- 已拿到执行资格
- 还是模型已经真正开始生成

用户看到的只有一个“转圈圈”，体验很差。

而你这个项目把排队状态也作为 SSE 事件流的一部分，这就意味着前端看到的不是一个死板 loading，而是一个真实的状态机。

从工程视角看，这是一种：

- 后端运行态
- 映射到前端用户态

的协议化设计。

### 10.6 为什么这个功能特别有含金量

因为它体现的是典型的生产级问题解决能力：

- 你知道模型调用是昂贵资源。
- 你知道多实例部署下单机锁不够。
- 你知道用户体验和系统保护要同时兼顾。

这已经不是简单业务功能，而是系统设计能力。

---

## 11. 高含金量功能八：标准熔断器接入模型路由

### 11.1 为什么要做熔断

如果某个模型已经明显出问题了，但系统还在疯狂继续调用它，会出现：

- 请求大量堆积
- 响应越来越慢
- 错误快速放大

这时最好的做法不是“继续硬试”，而是暂时跳过这个模型。

这就是熔断。

### 11.2 项目为什么从自写熔断升级到标准熔断

自己维护失败次数和冷却时间当然也能实现简单熔断。

但企业项目更偏向使用成熟组件，因为标准组件通常具备：

- 更稳定的状态机
- 更完整的指标
- 更清晰的半开恢复逻辑
- 更低的维护成本

所以项目后面接入了 Resilience4j 的标准熔断器。

### 11.3 什么是打开、半开、关闭

这是熔断器最重要的三个状态。

#### 11.3.1 关闭 Closed

正常状态，允许请求通过。

#### 11.3.2 打开 Open

说明最近失败率太高，系统暂时不再调用这个模型。

#### 11.3.3 半开 Half-Open

过了一段时间后，系统会少量放几个请求试试。

如果恢复正常：

- 回到 Closed

如果还不稳定：

- 再回到 Open

### 11.4 项目里做了什么

项目将模型调用包装在标准熔断器之中：

- 每个模型维护自己的熔断状态
- 被熔断拒绝时自动切换下一个模型
- 运行指标里能看到各模型的熔断状态和失败率

这和简单的“try-catch 然后换模型”不是一个层次的能力。

这里最好再补充一个常见误区：

很多人会说“我也做了模型降级”，但他的实现通常只是：

1. 调主模型
2. 报错
3. catch 住
4. 换备用模型

这只能算“失败后切换”。

而真正的标准熔断是：

- 根据一段时间内的失败率判断模型是否不健康
- 在明显不健康时主动拒绝新请求
- 过一段时间后再少量放量试探恢复

这就是为什么标准熔断的含金量更高，因为它不只是“会切换”，而是“知道什么时候不该继续试”。

### 11.5 为什么这一步很有含金量

因为它说明你理解的是：

- 高可用治理
- 失败隔离
- 标准组件化治理
- 系统恢复机制

这比“模型失败了就报错”高级很多。

---

## 12. 高含金量功能九：分级限流与 Redis 分布式限流

### 12.1 为什么限流不能一刀切

不同接口的资源成本完全不同。

比如：

- 登录接口和聊天流接口成本不同
- 上传接口和普通查询接口成本不同

如果所有接口都用同一套限流规则，会导致：

- 对重接口保护不够
- 对轻接口限制过严

### 12.2 项目里怎么做的

项目按不同接口类型设置不同限流桶：

- `stream`
- `upload`
- `auth`
- `default`

同时限流键还区分：

- 用户 ID
- 或者 IP
- 请求方法
- 请求路径

这就比“按 IP 全局限流”细得多。

### 12.3 为什么优先用 Redis，再回退本地 Bucket4j

多实例部署下，限流最好是分布式一致的。

所以项目优先使用 Redis 原子计数做分布式限流。

但如果 Redis 出问题，系统也不能直接失去保护能力。
所以又保留了本地 Bucket4j 作为兜底。

这就形成了：

- Redis 正常时，走分布式限流
- Redis 故障时，退回本机限流

这个设计很像真实生产环境里的“降级保护”。

### 12.4 为什么这一块有含金量

因为它说明你考虑了：

- 不同接口成本差异
- 多实例一致性
- 中间件故障兜底

这已经不是“会写过滤器”，而是懂系统保护策略。

---

## 13. 高含金量功能十：Token 精算与上下文滑动窗口

### 13.1 为什么不能无限堆历史消息

很多新手项目都有一个问题：

- 对话轮数越多
- 发送给模型的内容越长
- 成本越来越高
- 最后直接超过上下文上限

所以对话系统一定要做上下文治理。

### 13.2 Token 为什么不能用字符数代替

模型计费和上下文限制看的是 Token，不是字符数。

而字符数和 Token 并不是一回事：

- 一个汉字不一定等于 1 个 Token
- 一个英文单词也不一定等于 1 个 Token

如果估算不准，系统会：

- 低估成本
- 误判上下文长度
- 在请求真正发出去时才报错

所以项目用 JTokkit 做本地精准计算。

### 13.3 滑动窗口解决了什么

项目不是粗暴地“只保留最近 N 条消息”，而是：

- 优先保留重要上下文
- 在超过上限时逐步裁剪低优先级历史

这样可以在有限上下文中尽量保住有价值的信息。

### 13.4 为什么这个功能也有含金量

因为它对应的是大模型落地时最现实的问题之一：

- 成本控制
- 上下文长度治理
- 长会话可持续性

这不是炫技，而是非常务实的工程能力。

---

## 14. 高含金量功能十一：线程池隔离与异步任务拆分

### 14.1 为什么线程池不能混用

如果聊天生成、RAG 检索、摘要生成、工具执行都共用同一个线程池，会有一个经典问题：

- 某一类慢任务把线程池占满
- 其他任务一起被拖慢

这叫资源争抢。

### 14.2 项目里做了什么

项目把线程池拆成了多类：

- `chatExecutor`
- `ragExecutor`
- `summaryExecutor`
- `toolExecutor`

这样做的意义是：

- RAG 变慢，不一定拖垮聊天主链路
- 摘要任务变多，不一定阻塞工具执行
- 工具并发任务不会和检索抢同一批线程

### 14.3 为什么这个功能容易被忽略，但其实很值钱

因为很多简历项目只关注“有没有功能”，很少关注“这些功能同时运行时会不会互相打架”。

线程池隔离说明你开始站在系统稳定性的角度思考问题了。

---

## 15. 高含金量功能十二：运行时监控、历史指标与健康评估

### 15.1 为什么企业系统必须可观测

如果系统出问题了，但你没有观测能力，就会出现：

- 不知道是模型慢了
- 还是队列满了
- 还是熔断开了
- 还是 RAG 命中率下降了

所以一个企业系统必须具备“上帝视角”。

### 15.2 项目里做了哪些运行指标

项目对模型侧记录了：

- 活跃请求数
- 等待请求数
- 队列容量
- 排队超时数
- 请求拒绝数
- 成功数
- 失败数
- 熔断状态

对 RAG 侧记录了：

- 是否触发检索
- 命中率
- 无命中率
- 错误数
- 缓存命中率

而且这套 RAG 观测还有一个非常重要的价值：

> 它能帮你区分“是模型生成不行”，还是“前面的检索链路就已经出问题了”。

因为很多人遇到 AI 回答不好时，第一反应是“模型太弱”。
但真实原因往往可能是下面这些之一：

- 根本没触发 RAG
- query 改写质量不够
- 检索没有命中
- rerank 之后全部低于阈值
- 缓存命中了旧结果

如果没有观测，你只能凭感觉猜。

而有了这些指标以后，你至少能比较清楚地定位：

- 是没走检索
- 还是走了检索但没命中
- 还是命中了但质量不高

这就是为什么观测体系不是“锦上添花”，而是定位问题的基础设施。

### 15.3 为什么还要做历史序列

只看当前瞬时状态是不够的。

比如：

- 现在系统正常
- 但你想知道 10 分钟前是不是出过波峰

所以项目把运行时快照定时写入 Redis，形成历史序列。

这样就能查看：

- 最近一段时间的变化趋势
- 某次事故前后的状态变化

### 15.4 为什么还要做健康评估

管理员不一定想看一堆原始数字。

所以项目又把多个指标进一步汇总成：

- `healthy`
- `warning`
- `critical`

并给出告警列表和摘要。

这相当于把“原始监控”升级成了“可读的运行结论”。

### 15.5 为什么这一步很有含金量

因为它说明你不只是会写业务接口，还知道：

- 生产系统要能监控
- 要能留历史
- 要能快速定位问题
- 要把底层指标转成上层状态

这很像真实企业系统的运维思路。

---

## 16. 高含金量功能十三：JWT 权限体系与知识库权限边界

### 16.1 为什么 AI 系统也必须做权限控制

因为 AI 系统不是玩具，一旦接入：

- 用户私有知识
- 公司内部资料
- 全局公共知识

就一定会有权限问题。

如果权限做不好，后果比普通 CRUD 系统更严重，因为知识泄露通常更隐蔽。

### 16.2 项目里做了哪些权限控制

#### 16.2.1 登录鉴权

用户登录后会获得 JWT。

JWT 中包含用户身份和角色信息，后续请求靠它识别：

- 当前是谁
- 是普通用户还是管理员

#### 16.2.2 管理接口鉴权

管理员接口会校验角色，防止普通用户查看敏感运行信息。

#### 16.2.3 知识域权限边界

项目对知识域做了权限收口：

- 普通用户只能管理自己可访问的知识
- `global` 知识上传仅允许管理员
- 知识列表接口会根据角色返回不同可见范围

### 16.3 为什么这块很值钱

因为它体现了你不是只想着“功能能不能跑通”，还考虑了：

- 谁能上传
- 谁能看到
- 谁能删除
- 什么知识是全局共享

这已经很接近企业系统里的治理思维。

---

## 17. 这些功能之间是如何协同工作的

这一节非常重要，因为它能帮助你把项目讲成一个完整系统，而不是零散功能堆砌。

### 17.1 用户发起一次聊天时，系统大致会经历什么

1. 前端发起聊天请求。
2. 鉴权层识别当前用户和角色。
3. 限流器先判断是否超过频率限制。
4. `MemoryServiceImpl` 构建上下文：
   - 加系统提示词
   - 加历史消息
   - 加摘要
   - 判断是否要走 RAG
   - 如果要，就做三层知识域检索
5. `AiModelRouterServiceImpl` 开始调度模型：
   - 先过分布式并发闸门
   - 不够资格则排队
   - 获取资格后进入模型调用
   - 先尝试主模型
   - 如果失败则走降级链
   - 熔断打开则自动跳过不健康模型
6. 模型流式返回文本或工具调用事件。
7. 如果出现工具调用，则本地并行执行工具，再递归问模型。
8. 文本通过 SSE 持续推给前端。
9. 运行时指标被记录到监控模块。
10. 如果会话变长，则后台异步触发增量摘要。

如果你希望把这条链路讲得更像“企业系统”，可以再补一句：

这 10 步并不是几个功能随便拼在一起，而是一条“层层过滤、层层保护、层层增强”的流水线：

- 最前面先做身份校验和流量保护
- 中间做上下文构建和知识增强
- 后面做模型调度和稳定性治理
- 最后再做观测、统计和收敛

所以这个系统不是“大模型外面包几个功能”，而是一个真正有分层、有治理能力的 AI 后端。

### 17.2 为什么要理解“链路协同”

因为面试官真正想听的不是“你做了 13 个功能”，而是：

> 这些功能是如何串成一个企业系统的。

如果你能把这条链路讲清楚，项目的说服力会大幅提高。

---

## 18. 适合面试时重点讲的亮点

如果面试时间有限，建议优先讲这 8 个点：

### 18.1 Agent 工具调用

强调：

- 流式工具参数拼装
- 工具并行执行
- 递归调用模型
- 死循环保护

### 18.2 混合 RAG

强调：

- 向量检索 + BM25
- 并行召回
- 重排筛选
- 无命中降级

### 18.3 三层知识域

强调：

- `session / user / global`
- 作用域隔离
- 权限边界
- 召回优先级

### 18.4 增量摘要与 Token 治理

强调：

- 长会话成本控制
- 滑动窗口
- 摘要边界推进

### 18.5 模型高可用

强调：

- 主模型 + 自动降级链
- `deepseek` 保底
- 标准熔断器

### 18.6 分布式并发治理

强调：

- Redisson 分布式闸门
- 排队削峰
- SSE 队列状态提示

### 18.7 分级限流

强调：

- 按接口类型区分策略
- Redis 分布式限流
- 本地 Bucket4j 回退

### 18.8 运维可观测

强调：

- 运行时指标
- 历史序列
- 健康评估

---

## 19. 核心类导读

下面这些类，是你最应该熟悉的。

### 19.1 `ChatController`

作用：

- 聊天主入口
- 管理 SSE 连接
- 推送文本和状态事件

### 19.2 `AbstractOpenAiCompatibleModel`

作用：

- 对接兼容 OpenAI 协议的模型
- 解析流式返回
- 处理工具调用
- 递归生成最终回答

这是整个模型底层适配里最硬核的一层。

### 19.3 `MemoryServiceImpl`

作用：

- 构建模型上下文
- 处理 RAG 检索与缓存
- 管理摘要
- 记录 RAG 运行指标

如果把 RAG 说成“大脑”，这个类就是核心中的核心。

### 19.4 `AiModelRouterServiceImpl`

作用：

- 选择模型
- 执行降级链
- 控制并发
- 控制排队
- 接入熔断
- 输出运行指标

这是系统高可用能力的主战场。

### 19.5 `ChatAttachmentServiceImpl`

作用：

- 文件上传解析
- 向量入库
- 知识删除闭环
- 知识管理服务

### 19.6 `RateLimitFilter`

作用：

- 分级限流
- Redis 分布式计数
- 本地桶降级保护

### 19.7 `RuntimeHealthServiceImpl`

作用：

- 把原始运行指标转换成健康结论
- 生成告警摘要

---

## 20. 这个项目最适合如何包装成简历项目

你可以这样描述：

> 基于 Spring Boot 构建企业级 AI 智能助手系统，集成流式对话、Agent 工具调用、混合 RAG、三层知识域、增量摘要、分布式并发闸门、标准熔断、分级限流和运行时监控能力，实现模型高可用与知识库治理闭环。

### 20.1 参考常见高质量简历项目经历，应该怎么写这个项目

你发的那类项目经历有一个共同特点：

- 不是只写“做了什么功能”，而是先写清楚“系统原来有什么痛点”。
- 不是只堆技术名词，而是强调“用了什么方案解决什么问题”。
- 不是停留在实现动作，而是尽量补一句“最终效果是什么”。

也就是说，真正更容易打动面试官的写法，一般遵循这个顺序：

1. 业务或系统痛点
2. 关键设计方案
3. 核心落地实现
4. 最终效果或工程价值

你可以把它理解成一个非常通用的表达公式：

> 针对什么问题，设计了什么方案，怎么落地，最后把系统提升到了什么程度。

这个公式特别适合当前项目，因为这个项目的含金量，本来就不在“页面多漂亮”，而在于它补了很多企业级 AI 系统落地时真正会遇到的坑。

### 20.2 当前项目如果按“问题 - 方案 - 效果”来写，最值得强调什么

如果把当前项目压缩成更像简历语言的表达，最值得优先保留的不是所有功能，而是下面 6 类高价值能力：

1. RAG 质量治理

- 解决普通 RAG 容易机械切分、检索不准、回答不够 grounded 的问题。
- 方案是结构化分块、多查询扩展、向量检索 + BM25 混合召回、Rerank 精排、低分过滤和无命中降级。

2. 知识域隔离与治理闭环

- 解决企业知识不能混用、不同用户数据边界不清的问题。
- 方案是构建 `session / user / global` 三层知识域，并补齐上传、检索、删除、缓存失效和权限边界闭环。

3. 模型高可用治理

- 解决单模型不稳定、失败后整条链路受影响的问题。
- 方案是主模型轮换与自动降级、Redisson 分布式并发闸门、请求排队削峰、Resilience4j 标准熔断。

4. 长会话成本治理

- 解决聊天轮数增加后上下文越来越长、token 成本越来越高的问题。
- 方案是 Token 精算、滑动窗口截断、增量摘要和辅助 AI 子任务资源隔离。

5. 系统安全与稳态治理

- 解决 AI 系统高并发下容易被打爆、接口边界不清的问题。
- 方案是 JWT 鉴权、角色权限控制、按用户/IP + 方法 + 路径维度的分级限流，并支持 Redis 优先、本地桶降级。

6. 可观测性与质量闭环

- 解决系统运行后“出问题不知道出在哪、回答好坏无法量化”的问题。
- 方案是补齐运行态指标、历史序列、健康评估、点赞点踩反馈统计和管理接口。

### 20.3 当前项目的项目经历长版写法

如果你想写成更接近图片里的“项目经历”风格，可以直接参考下面这一版：

**项目名称：** 企业级 AI 智能助手系统  
**技术栈：** Spring Boot + Spring AI + MyBatis-Plus + Redis + Redisson + MySQL + SSE + JWT  
**项目描述：** 针对普通 AI 对话系统存在知识问答不可信、RAG 检索质量不稳、单模型易受流量冲击、长会话成本高和系统治理能力弱等问题，设计并实现集对话编排、Agent 工具调用、混合 RAG、模型高可用治理、权限控制与运行监控于一体的企业级 AI 后端系统。

**核心工作与亮点：**

1. 针对传统 RAG 机械切分、语义鸿沟和 Prompt 约束弱的问题，重构知识库链路，采用结构化分块 + 多查询扩展 + 向量检索/BM25 双路并行召回 + Rerank 精排 + 低分过滤 + 无命中降级，提升知识问答命中质量与回答可信度。
2. 针对企业知识混用和数据边界不清的问题，设计 `session / user / global` 三层知识域隔离机制，打通知识上传、检索、删除、向量文档回收、缓存失效和权限控制闭环，提升多用户场景下的数据安全性与知识治理能力。
3. 针对大模型调用在高并发场景下容易排队失控、单点失败和雪崩放大的问题，引入主模型自动降级链、Redisson 分布式并发闸门、请求排队削峰和 Resilience4j 标准熔断，提升模型调用链路的稳定性与可恢复性。
4. 针对长会话上下文膨胀和 token 成本过高的问题，设计 Token 精算、滑动窗口截断、增量摘要和辅助 AI 子任务资源隔离机制，在保证对话连续性的同时降低上下文冗余与资源竞争。
5. 针对 AI 系统上线后的运维和质量评估难题，补齐分级限流、运行态指标、历史监控、健康评估以及回答点赞/点踩反馈统计，形成从运行稳定性到回答质量的基础治理闭环。

这类写法的优点是：

- 面试官一眼能看到你解决的是“真实工程问题”。
- 不会显得只是在罗列 AI 术语。
- 每一点都能往下展开成一段面试回答。

### 20.4 当前项目的项目经历精简版

如果你想写成更适合简历一小段的位置，可以直接用下面这个版本：

> 基于 Spring Boot 与 Spring AI 搭建企业级 AI 智能助手系统，围绕普通 AI 项目中知识问答不可信、RAG 质量不稳、模型调用易受高并发冲击等问题，设计并落地结构化分块、多查询扩展、混合检索与 Rerank 的高可信 RAG 链路，同时实现 `session / user / global` 三层知识域隔离、Redisson 分布式并发闸门、自动降级与标准熔断、增量摘要、分级限流和运行监控，提升系统在知识治理、稳定性和可运维性方面的工程落地能力。

### 20.5 当前项目的项目经历超精简版

如果简历空间特别小，可以再压成这一版：

> 设计并实现企业级 AI 智能助手系统，围绕 RAG 可信性、模型高可用和系统治理三条主线，落地结构化分块、混合检索、三层知识域、分布式并发闸门、标准熔断、增量摘要和分级限流，提升 AI 系统在多用户、多知识域和高并发场景下的稳定性与可落地性。

如果面试官继续追问，你再展开：

- 为什么做三层知识域
- 为什么做分布式闸门
- 为什么要把自写熔断升级为 Resilience4j
- 为什么要做 RAG 门控、缓存和增量摘要

如果面试官继续深挖 RAG，你还可以补讲这 4 个升级点：

- 为什么固定长度切分不够，必须升级到结构化切分
- 为什么要给 chunk 保存 `source_path`
- 为什么单个 query 不够，要做多查询扩展
- 为什么 Prompt 不能只写“请根据资料回答”

这样你的项目叙述会非常完整。

---

## 21. 面试时容易被问到的追问与回答思路

这一节是给你准备的“实战版文档”，帮助你把项目真正讲顺。

### 21.1 面试官问：你这个 RAG 和普通 RAG 有什么区别？

你可以这样回答：

普通 RAG 往往只做了“上传文档 + 向量检索 + 拼接上下文”。

而我的项目在几个关键点上做了增强：

1. 检索前先做意图门控，不是所有问题都盲目走知识库。
2. Query 不只是拿用户原话，而是结合摘要和上下文做改写。
3. 进一步做多查询扩展，覆盖口语化和书面化差异。
4. 检索不是单路向量，而是向量 + BM25 混合检索。
5. 召回后不是直接喂模型，而是先做 rerank 和阈值过滤。
6. Prompt 不是弱约束，而是严格 grounded，要求带来源回答。

这样回答时，面试官会明显感觉到你不是“只会接个向量库”。

### 21.2 面试官问：为什么不直接把整篇文档喂给模型？

你可以从 3 个角度回答：

1. 成本太高：长文档直接喂模型会消耗大量 token。
2. 粒度太粗：模型很难在整篇文档里精准定位问题对应内容。
3. 上下文污染：很多内容和当前问题无关，只会浪费上下文窗口。

所以企业里更常见的做法是：

- 先切分
- 再检索
- 再只把最相关的片段交给模型

### 21.3 面试官问：为什么要做结构化切分？

你可以回答：

因为很多文档的语义边界不是按字数划分的，而是按章节、标题和小节划分的。

如果只按固定长度切分，会把一个完整流程拆碎，导致模型检索到不完整步骤。

所以我把切分升级成了：

- 优先尊重文档结构
- 章节过长时再做二次分片
- 同时保留结构化来源路径

### 21.4 面试官问：为什么要做多查询扩展？

你可以回答：

因为用户说话方式和文档写法经常不同。

用户可能说：

- “数据库挂了咋办”

而文档可能写的是：

- “MySQL 服务异常处理”

如果只用一个 query，检索覆盖面不够。
所以系统会生成多个表达变体，再统一检索和重排。

### 21.5 面试官问：为什么还要做 Rerank？

你可以回答：

召回阶段的目标是“尽量别漏”，不是“排序一定最准”。

但真正送给模型的上下文数量有限，所以必须在召回后再做一轮更细粒度的相关性排序。

这一步能显著降低无关片段进入最终上下文的概率。

### 21.6 面试官问：为什么不能让模型在没检索到资料时自己补充常识？

你可以回答：

这取决于产品目标。

如果是通用聊天，允许模型补充常识未必有问题。
但如果是企业知识问答，最重要的是“可信”和“可追溯”。

所以在我的项目里，当知识库证据不足时，会优先要求模型明确说明无相关信息，而不是瞎猜。

这是一种更稳妥、更企业级的策略。

---

## 22. 总结

这个项目最强的地方，不是功能数量多，而是它围绕大模型系统真正难落地的点，补齐了很多关键工程能力：

- 模型不可靠，所以做了降级、熔断、排队和分布式并发治理。
- 模型会胡说，所以做了混合 RAG、重排、知识域隔离和知识治理。
- 大模型贵且上下文有限，所以做了 Token 精算、滑动窗口和增量摘要。
- 企业系统要能运营，所以做了权限控制、监控历史和健康评估。

如果你是初学者，这份项目能帮助你建立“AI 后端不是简单调接口”的正确认知。

如果你是准备面试的人，这个项目已经具备很强的讲述空间，因为它不仅有 AI 能力，也有后端架构能力和系统治理能力。

> 一句话结论：这不是一个只会聊天的 AI Demo，而是一个围绕真实生产问题持续演进出来的企业级 AI 后端系统。
